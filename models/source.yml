version: 2

sources:
  - name: sources
    database: mack-project-425820
    schema: transactions
    tables:
      - name: financial_transaction
        meta:
          contains_pii: true
        columns:
          - name: card_number
            meta:
              contains_pii: true

          - name: Card_CVV
            meta:
              contains_pii: true
              